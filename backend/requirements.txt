# EvacuWay backend — core runtime dependencies (install on Python 3.11+).
# These are sufficient to run the API, the simulation engine and the full
# 270-run statistical experiment with the bundled prototype graph.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
python-multipart>=0.0.9
numpy>=1.26
pandas>=2.1
networkx>=3.2
scipy>=1.11
statsmodels>=0.14
openpyxl>=3.1

# --------------------------------------------------------------------------- #
# Optional — full Metro Manila OSMnx graph extraction (production pipeline).
# Not required for the prototype simulation or the smoke test. Install with:
#     pip install -r requirements-geo.txt
# osmnx>=1.9
# geopandas>=0.14
# shapely>=2.0
# mesa>=2.1
