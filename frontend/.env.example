# EvacuWay frontend environment template. Copy to .env if you need to override.
# Points the dashboard at the FastAPI backend. When the API is unreachable the
# dashboard automatically falls back to the bundled sample data in /public.
VITE_API_URL=http://localhost:8000
